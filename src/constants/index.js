// Central theme exports for consistent theming across the app
export { useTheme, ThemeProvider, modernColors, safeColors } from "./Theme";

// Re-export everything from Theme for backward compatibility
export * from "./Theme";
